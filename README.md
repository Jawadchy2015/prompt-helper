# Prompt Helper MVP (Local)

## Run backend
cd server
npm install
npm run dev

## Load extension
Chrome -> chrome://extensions -> Developer mode -> Load unpacked -> select ./extension

Then open https://chatgpt.com and type in the prompt box.
Wait ~500ms after typing to see ghost suggestion. Press Tab to accept, Esc to dismiss.
